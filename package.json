{
  "name": "esimcn-net",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply esimcn-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply esimcn-db --remote"
  },
  "dependencies": {
    "hono": "^4.12.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260405.1",
    "node-addon-api": "^8.7.0",
    "node-gyp": "^12.2.0",
    "typescript": "^6.0.2",
    "wrangler": "^4.80.0"
  }
}
